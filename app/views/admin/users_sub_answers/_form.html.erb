<%= form_for [:admin,@users_sub_answer] do |f| %>
<% if @users_sub_answer.errors.any? %>
<div id="error_explanation" class="alert alert-danger">
	<a href="#" class="close" data-dismiss="alert">&times;</a>
	<h2><%= pluralize(@users_sub_answer.errors.count, "error") %>
		prohibited this sponsor from being saved:</h2>
	<ul>
		<% @users_sub_answer.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>
<div class="form-group">
	<%= f.label :users_sub_question_id %>
	<%= f.select :users_sub_question_id,options_from_collection_for_select(User.where(:enable=>true).all, 'id', 'name',@users_sub_answer.user_sub_question_id),{},{:class=>"form-control"} %>
</div>
<div class="form-group">
	<%= f.label :sub_answer_id %>
	<%= f.select :sub_answer_id,options_from_collection_for_select(SubAnswer.where(:enable=>true).all, 'id', 'title',@users_sub_answer.sub_answer_id),{},{:class=>"form-control"} %>
</div>
<div class="form-group">
	<%= f.label :enable %>
	<%= f.check_box :enable %>
</div>
<div class="form-group actions">
	<%= f.submit :class=>"btn btn-primary" %>
</div>
<% end %>
