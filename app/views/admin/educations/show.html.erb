<div class="sl_show">
	<p>
		<b><%=t(:label_title) %></b>
		<%= @education.title %>
	</p>
	<p>
  	<b><%=t(:label_created_at) %></b>
  	<%= @education.created_at %>
  </p>
  <p>
  	<b><%=t(:label_updated_at) %></b>
  	<%= @education.created_at %>
  </p>
</div>
<% if can? :update, @education %>
<%= link_to t(:link_edit), edit_admin_education_path(@education) %> |
<% end %>
<%= link_to t(:link_list), admin_educations_path %>
